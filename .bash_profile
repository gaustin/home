export PATH="/Users/gaustin/bin:/usr/local/bin:$PATH"

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

# RVM told me to do this. Seems to be loaded already on OS X.
#source ~/.profile
